#!/bin/bash
set +x

for script in $(ls -1 githooks/pre-commit-scripts 2> /dev/null); do
  ts=$SECONDS
  printf "Running githook: $script \n"
  bash githooks/pre-commit-scripts/$script
  te=$(($SECONDS - $ts))
  printf "Script Took (${te}s)\n"
done

printf "pre-commit complete \n\n"
